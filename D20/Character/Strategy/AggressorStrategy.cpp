#include "AggressorStrategy.h"
#include "../Character.h"

void AggressorStrategy::performMove(Character& character) {
    // Implement logic for aggressor to move towards player character
}

void AggressorStrategy::performAttack(Character& character) {
    // Implement logic for aggressor to attack the player character
}

void AggressorStrategy::performFreeActions(Character& character) {
    // Aggressor doesn't take any free actions
}
